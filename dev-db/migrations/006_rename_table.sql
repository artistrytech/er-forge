-- テーブルのリネーム（K-09 のリネーム候補・テーブル編。この設計の要）。
--
-- カラム構成シグネチャの照合でリネーム候補として提示される。承認すると:
-- - meta（テーブル論理名・タグ・注記・論理制約）が引き継がれる
-- - 他テーブルの FK 参照が追随する
-- - **全ページの ER図ノードのキーが書き換わり、座標は変わらない**（配置が失われない）
-- 却下すると「削除 + 追加」になり、配置も論理名も失われる（差分プレビューが警告する）。

ALTER TABLE public.point_campaigns RENAME TO point_promotions;

COMMENT ON TABLE public.point_promotions IS 'ポイント販促キャンペーン';
