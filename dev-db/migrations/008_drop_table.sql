-- テーブルの削除（K-08 の「削除」/ K-13 の孤児ノード）。
--
-- 適用すると schema/**.js は消えるが、**ER図のノードは自動削除されない**。
-- 参照先を失ったノードは「孤児」として警告色で残り、除去は人の明示操作でのみ行う
-- （内省スコープの設定ミスで「削除」に見えているだけかもしれないため。配置は DB から復元できない）。
--
-- 削除が既存の 50% 以上になるとガードが働き、適用前に追加確認を求められる（K-08）。

DROP TABLE public.user_sessions;
